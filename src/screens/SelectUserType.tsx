import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const SelectUserType = ({navigation}) => {
  return (
    <View>
      <Text>SelectionUserType</Text>
    </View>
  );
};

export default SelectUserType;

const styles = StyleSheet.create({});
