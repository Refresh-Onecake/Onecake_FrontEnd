import {SafeAreaView, StyleSheet, Text, View} from 'react-native';
import React from 'react';

export const EnterMenuSheet = () => {
  return (
    <SafeAreaView>
      <Text>enterMenuSheet</Text>
    </SafeAreaView>
  );
};

const styles = StyleSheet.create({});
